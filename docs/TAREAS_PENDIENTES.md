# Tareas pendientes — sistema de agentes (consolidado)

> Único documento de tareas del proyecto. Antes esto vivía repartido entre `docs/roadmap.md` (sección "Backlog abierto"), `AGENTE_IMPORTADOR.md` en Bckbs y un encargo aparte (`BRIEF_registro_alergias_intolerancias.md`) — se consolida aquí para no mantener la misma tarea descrita en varios sitios a la vez. `roadmap.md` sigue siendo el documento de arquitectura/diseño; este es el documento de seguimiento de trabajo pendiente.
>
> **Última actualización:** 2026-09-19 (ítem 2.5 cerrado del todo — Hamsa y Ayoub completados, confirmado por código que la app ya soporta las 4 preguntas sin cambios; ítem 2.4 cerrado — `nivel_fuerza` confirmado por el coach para los 6 clientes reales, la regla de derivación falló en la mitad de los casos y deja de usarse como fuente de verdad; ítem 2.9 cerrado el mismo día que se abrió — alergia de Borja a piel de fruta confirmada como leve, corregida en Bckbs y en `bstronger-memoria-clientes`, corrigiendo también una confusión con "lactosa" introducida por esta misma sesión; los 5 clientes con perfil real ampliados con `docs/USERS/NUTRICIÓN`/`RECETARIO` del repo `bsa` -- ver "Ya resuelto"; varios hallazgos de calidad de datos en esos documentos, ninguno bloqueante pero sí relevante; fusión de `perfil-cliente.json`/`perfil-nutricional.json` en `bstronger-memoria-clientes`; perfiles de Osas y Alberto Martín creados sin PAR-Q real, ampliados con sus guidelines de coach; nuevo ítem 2.8 — Josu Mauricio no tiene cuenta en Bckbs, bloqueado hasta que el usuario confirme. Estado previo, 2026-09-18: 1.1 y 1.2 cerradas del todo; 1.4 resuelto — volcado inicial de 6 perfiles reales a `bstronger-memoria-clientes`, ampliado después con 3 guidelines de coach reales (Hamsa, Toni, Borja); 2.3 cerrado del todo — Toni confirmó lactosa como intolerancia/leve, ambas restricciones suyas ya en `client_limitations` de Bckbs; 2.5 parcial — Borja/Toni rellenados a mano, sigue sin decisión sistemática para el resto; 2.6 y 2.7 resueltos)

## Cómo leer esta tabla

| Columna | Significado |
|---|---|
| Estado | 🔒 Bloqueada (no se puede avanzar desde esta sesión) · 🟡 Requiere el usuario (dato/decisión que solo puede dar él) · ⏸️ Pospuesta a propósito (no se escribe por completitud especulativa) · 🟢 Mejora opcional (no bloqueante) |
| Bloqueador | Qué hace falta exactamente para desbloquearla |

---

## 1. Bloqueadas por acceso a BD/VPS real (`bestronger-vps`)

**(2026-09-17)** Varias sesiones en paralelo tuvieron acceso SSH real a `bestronger-vps` (`/var/www/testapp`, `APP_ENV=production`, BD `bestronger_test`, la BD real de `testapp.bestronger.es` pese al nombre). 1.2 estaba marcada como bloqueada aquí y **ya no lo estaba** — alguien la aplicó ya en producción, probablemente durante el trabajo de integración de onboarding del 2026-09-16/17. 1.1 se cerró del todo con un catálogo `.xlsx` nuevo y una norma de asignación manual establecida (ver "Ya resuelto"). Detalle en cada fila.

| # | Tarea | Estado | Bloqueador | Detalle completo |
|---|---|---|---|---|
| 1.1 | Verificar contra BD real todo lo mergeado a `main` de Bckbs (`--json`, `programs:assign-client`, `POST program-import`, hotfix `fail()`→`reportFailure()`, y ahora también `--confidence-gate`/`--check-integrity`, ver 4.1/4.2) | ✅ Resuelto — ver "Ya resuelto" | — | `Bckbs/docs/AGENTE_IMPORTADOR.md`, sección "Tarea pendiente" |
| 1.2 | Construir el registro estructurado de severidad de alergias/intolerancias en Bckbs (`client_limitations`: columna `severity`, ampliar `type`, validación 422 si `type=allergy` sin `severity`, tests) | ✅ Resuelto — ver "Ya resuelto" | — | `BRIEF_registro_alergias_intolerancias.md` |
| 1.3 | Etiquetado de alérgenos por ingrediente/receta en Bckbs (`ingredients`/`recipes` no tienen columna tipo `contiene_gluten`) | 🔒 | Depende de 1.2 (ya resuelta) — sigue sin escribirse porque no hay un caso real que lo necesite todavía, no por bloqueo técnico | `agentes/programacion-nutricion/formato-salida/entrega-bckbs.md`, sección 4 (limitación documentada); `BRIEF_registro_alergias_intolerancias.md`, sección 6 ("fuera de alcance") |
| 1.4 | Volcado inicial: crear `perfil-cliente.json`/`perfil-nutricional.json` en `bstronger-memoria-clientes` para todos los clientes con onboarding real completo, antes de generar ninguna rutina/plan | ✅ Resuelto — ver "Ya resuelto" | — | `VOLCAR_PERFILES_ONBOARDING.md` |

---

## 2. Bloqueadas por datos o decisión del usuario (no técnicas)

| # | Tarea | Estado | Bloqueador | Detalle completo |
|---|---|---|---|---|
| 2.1 | Definir `perfil-cliente.schema.json`/`perfil-nutricional.schema.json` con datos reales de clientes actuales | 🟡 (parcial) | **(2026-09-16) La estructura ya se reconcilió** contra las columnas reales del onboarding (`par_q_answers`/`training_questionnaire_answers`/`nutrition_questionnaire_answers`) sin necesitar datos de ningún cliente — el usuario confirmó que el onboarding real queda en BD, así que se comparó el esquema contra el schema de esas tablas directamente. Queda pendiente contrastar con VALORES reales (no solo la forma) de 2-3 clientes actuales, para detectar patrones que la estructura por sí sola no revela | `agentes/programacion-entrenamiento/esquemas/perfil-cliente.schema.json`, `agentes/programacion-nutricion/esquemas/perfil-nutricional.schema.json` |
| 2.2 | Caso real de cliente de principio a fin para el Asistente de Nutrición (equivalente al caso de Toni en entrenamiento) | 🟡 (parcial) | **(2026-09-19)** Ya existen recetarios reales usables (`docs/USERS/RECETARIO/` en `bsa`, ver "Ya resuelto" de esa fecha) y se han volcado como referencia a `bstronger-memoria-clientes`. Falta el paso que de verdad cierra el ítem: cruzar esas recetas (nombre + macros en texto libre) contra el catálogo real de Bckbs (`GET recipe-filter-list`/`recipe-detail/{id}`) para obtener `recipe_id` real y construir el fixture con la forma exacta que espera `validador/validar_plan.py` — trabajo de matching análogo al que hace el Agente Importador con ejercicios, todavía sin hacer | `agentes/programacion-nutricion/validador/README.md` (fixtures hoy sintéticos, sin caso real) |
| 2.3 | Migrar a `client_limitations` las alergias que hoy solo existen como texto libre en `nutrition_questionnaire_answers.allergies_intolerances` | ✅ Resuelto — ver "Ya resuelto" | — | `BRIEF_registro_alergias_intolerancias.md`, sección 5 |
| 2.4 | Confirmar o ajustar la regla de derivación de `nivel_fuerza` | ✅ Resuelto — ver "Ya resuelto" | — | `agentes/programacion-entrenamiento/esquemas/perfil-cliente.schema.json`, campo `experiencia_entrenamiento.nivel_fuerza` |
| 2.5 | Decidir qué hacer con clientes que completaron el onboarding antes del 2026-09-16 | ✅ Resuelto — ver "Ya resuelto" | — | `Bckbs`, migraciones `2026_09_16_100000`/`2026_09_16_100001` |
| 2.6 | Decidir dónde viven físicamente los archivos reales de memoria por cliente (`log-registro`/`log-nutricion`/`checkpoint-fisico`) | ✅ Resuelto — ver "Ya resuelto" | — | `agentes/programacion-entrenamiento/system-prompt.md`, sección "Memoria del cliente" |
| 2.7 | Añadir una capa de "hábitos prioritarios" a alguno de los dos agentes | ✅ Resuelto — ver "Ya resuelto" | — | `agentes/programacion-nutricion/modulos/habitos-prioritarios.md` |
| 2.8 | Josu Mauricio (`docs/USERS/GUIDELINE/guideline-josu-mauricio.html`, repo `bsa`) no tiene ninguna cuenta de usuario en Bckbs — verificado por SSH real contra `bestronger-vps` (2026-09-19): sin coincidencia por `first_name`, `last_name`, `username` ni `email` conteniendo "josu"/"mauricio". No se puede crear su perfil en `bstronger-memoria-clientes` sin un `user_id` real (convención `<id>-slug`, ver `bstronger-memoria-clientes/README.md`) | 🟡 | ¿Tiene cuenta real con otro nombre/email, o todavía no se ha registrado en la app? Sin esto no hay `cliente_id` válido que asignarle | `bstronger-memoria-clientes/clientes/` (sin carpeta para él) |
| 2.9 | Borja (101) — alergia a piel de fruta | ✅ Resuelto — ver "Ya resuelto" | — | `bstronger-memoria-clientes/clientes/101-borja-betanzos/perfil-cliente.json` |

---

## 3. Pospuestas a propósito (no se escriben por completitud especulativa)

Mismo criterio en los dos agentes: un módulo de población/deporte específico solo se escribe cuando existe un cliente real que lo necesita, nunca por anticipado.

| # | Tarea | Agente | Estado |
|---|---|---|---|
| 3.1 | Módulos de población/deporte específicos (fútbol, otros deportes de equipo) | Entrenamiento | ⏸️ |
| 3.2 | Módulo de poblaciones específicas (embarazo, patologías concretas) | Entrenamiento | ⏸️ |
| 3.3 | Módulo de poblaciones específicas (embarazo, patologías concretas) | Nutrición | ⏸️ |

---

## 4. Mejoras opcionales no bloqueantes

Anotadas para no perderlas, pero ninguna es un hueco crítico hoy — el diseño actual (Human-in-the-Loop) ya cubre el riesgo que resolverían.

| # | Tarea | Estado | Nota |
|---|---|---|---|
| 4.1 | `programs:check-integrity` inmediato tras cada import real, no solo cron semanal | 🟢 (en `main`) | **(2026-09-16) Fusionado a `main`.** `--check-integrity` (CLI) / `check_integrity` (HTTP), opcional y retrocompatible. **(2026-09-17)** Ejecutado standalone (sin `--fix`) contra la BD real: "Sin referencias rotas. Todo correcto (207 filas revisadas)." Sigue faltando probarlo encadenado inmediatamente tras un import real (no hubo import real que disparara ninguno, ver 1.1). `Bckbs/docs/AGENTE_IMPORTADOR.md`, sección 7, punto 4 |
| 4.2 | Umbral de revisión humana configurable por nivel de confianza (hoy `review_required` ya separa A/B de C/D/E, pero la pausa la impone el LLM siguiendo el system-prompt, no la CLI) | 🟢 (en `main`, ya no es el flujo por defecto) | **(2026-09-16) Fusionado a `main`.** `--confidence-gate` (CLI) / `confidence_gate` (HTTP): antes de un import real, corre un dry-run en memoria y aborta sin escribir si hay ejercicios sin revisar. 10 tests contra MySQL real local. **(2026-09-17)** Confirmado con un caso real de match ambiguo contra un catálogo nuevo (`nerea-media-m1.xlsx`, ver 1.1): detectó correctamente un match nivel D (0.74) semánticamente incorrecto (RDL unipodal con kettlebell vs RDL bilateral con barra) más 7 auto-creaciones. Tras la norma nueva establecida en 1.1 (import automático, pausa movida a la asignación), `--confidence-gate` deja de ser el flujo por defecto del agente importador — sigue disponible como opción, no como guardrail obligatorio. `Bckbs/docs/AGENTE_IMPORTADOR.md`, sección 7, punto 5 |
| 4.3 | Comando/endpoint de importación de planes de nutrición a Bckbs (análogo a `programs:import`) | 🟢 (depende de 1.2) | `BRIEF_registro_alergias_intolerancias.md`, sección 6 ("fuera de alcance") — no hace falta hoy porque el Productor de nutrición ya escribe directo vía la API existente (`formato-salida/entrega-bckbs.md`) |

---

## Ya resuelto (referencia, no acción)

- Los cinco bloqueantes originales del Agente Importador de Programas (`--json`, endpoint HTTP, `programs:assign-client`, `check-integrity` en cron, `review_required`).
- Formato de salida y recetario real del Asistente de Nutrición — la API de Bckbs (`meal_plan_templates`/`daily_plan_recipes`, `recipe-filter-list`) ya existía, no hizo falta construir nada nuevo.
- Deep search de contenido científico del Asistente de Nutrición: necesidades energéticas/macros, timing, recomposición corporal, superávit de ganancia muscular, rendimiento deportivo/resistencia, y evidencia clínica de alergias/intolerancias.
- **(2026-09-16) Cribado de seguridad faltante en el onboarding real** — `par_q_answers` no preguntaba embarazo/posibilidad, alteración menstrual/fractura por estrés (RED-S) ni trastorno alimentario, pese a que `contraindicaciones-medicas.md` lo asumía desde el diseño. Añadido a Bckbs (migración + validación + flag de revisión + tests).
- **(2026-09-16) `disponibilidad_cocina` en el onboarding de nutrición** — requerido por el esquema desde el primer borrador, nunca se preguntaba. Añadido a Bckbs.
- **(2026-09-16) Disponibilidad de entrenamiento editable post-onboarding** — nuevo endpoint `POST training-availability-update`, sin repetir todo el cuestionario.
- **(2026-09-17) Integración en la app de los tres cambios de onboarding** — el usuario confirmó que ya está integrado: las preguntas de embarazo/RED-S se muestran solo a perfil mujer, disponibilidad de cocina añadida a la etapa 4, y la pantalla de disponibilidad de entrenamiento editable. La PR `feature/onboarding-safety-and-preferences` de Bckbs está fusionada a `main` (verificado: 59 tests relevantes en verde, sin conflicto con el trabajo de otras sesiones — Panel de Tareas, readiness-scores).
- **(2026-09-17) 1.1 — Agente Importador, cerrado del todo contra BD real, y norma de asignación manual establecida.** Se generó un mesociclo real nuevo (`nerea-media-m1.xlsx`, transcrito del programa real de una clienta de media maratón — bloque fuerza/potencia/pliometría, 4 semanas × 3 sesiones/semana, 68 filas) y se subió a `storage/imports/` del VPS para tener por fin un catálogo no importado antes. `--dry-run --json --confidence-gate` contra él sí ejercitó el gate con casos ambiguos reales (no solo matches perfectos como con los dos catálogos ya importados): un match nivel D con confianza 0.74 (`Peso muerto rumano a una pierna con kettlebell` → matcheado contra `Peso muerto rumano con barra`, unilateral/kettlebell confundido con bilateral/barra — exactamente el tipo de error de prescripción silencioso que `ExerciseMatcher` puede cometer, ver sección 5) más 7 ejercicios sin match suficiente que se auto-crearían. **Decisión explícita del usuario (2026-09-17): se establece una norma nueva que sustituye el principio de diseño anterior** — el import real se ejecuta automáticamente aunque haya `review_required` (crear el programa y los ejercicios de catálogo, incluidos auto-creados/ambiguos, es de bajo riesgo porque ningún cliente lo ve todavía); la pausa humana no negociable se mueve a la asignación — **el agente importador nunca ejecuta `programs:assign-client`/`POST training-program-assign-client` por su cuenta**, eso lo hace siempre el humano a mano desde el panel admin tras revisar el programa recién creado. Import real ejecutado end-to-end sobre `nerea-media-m1.xlsx`: `training_program #55` creado (4 semanas, 27 ejercicios nuevos, 4 matches nivel D, 4 nivel A, 28 nivel B), `0` asignaciones a cliente (confirmado con `ProgramClientAssignment::where('training_program_id',55)->count()` → `0`), `check-integrity` limpio inmediatamente después (270 filas revisadas, sin referencias rotas). Norma documentada en `Bckbs/docs/AGENTE_IMPORTADOR.md`, secciones "Flujo paso a paso" y "Principio de diseño no negociable" (ambas reescritas con fecha).
- **(2026-09-18) 2.3 — Toni Pérez Fernández (user_id 99), cerrado del todo.** El usuario confirmó `lactosa` como `type: intolerance`, `severity: mild` ("leve"). Creadas dos filas en `client_limitations` de Bckbs: `id=3` (alergia a fruta/fructosa, `severity: severe_anaphylaxis`, confirmada por un guideline de coach real que la marca como crítica) e `id=4` (intolerancia a lactosa, `severity: mild`). Ambas reflejadas también en `bstronger-memoria-clientes`. Ítem 2.3 queda resuelto del todo (antes solo Borja).
- **(2026-09-17) 1.4 — volcado inicial de perfiles reales, 6/6 clientes procesados.** Ejecutado desde una sesión con acceso SSH real a `bestronger-vps`, siguiendo `VOLCAR_PERFILES_ONBOARDING.md` al pie de la letra. Solo lectura contra Bckbs (`par_q_answers`/`training_questionnaire_answers`/`nutrition_questionnaire_answers`/`client_limitations`), escritura solo en `bstronger-memoria-clientes` (commit `00330f4`). Los 6 usuarios con `par_q_answers` + `training_questionnaire_answers` en la BD real tenían también `nutrition_questionnaire_answers`, así que los 6 tienen `perfil-cliente.json` + `perfil-nutricional.json`. Ningún dato de seguridad inventado: `parq_eating_disorder_history` queda `null` + pendiente en los 6 (pregunta nunca hecha, onboarding anterior al 2026-09-16); Toni sigue sin `tipo`/`severidad` para "fruta/lactosa" (ver 2.3); `nivel_fuerza` marcado `_provisional` en los 6 (ver 2.4); `disponibilidad_cocina` a `null` en los 6 (ver 2.5). Resumen completo para el coach en `bstronger-memoria-clientes/clientes/_pendientes-volcado-2026-09-17.md`, incluyendo aviso de que 2 de las 6 cuentas (`8-hamzaa-cliente`, `100-hamza-elouafa`) parecen ser demo/de prueba — decisión del usuario si se excluyen.
- **(2026-09-17) 1.2 — severidad de alergias/intolerancias, confirmada en producción.** Verificado con acceso SSH real al VPS: migración `2026_09_16_090000_add_severity_to_client_limitations_table` en estado `Ran` (batch 36), columna `severity` presente en `client_limitations` (`SHOW COLUMNS`), `type` ampliado a `injury,limitation,medical_condition,allergy,intolerance,aversion,ethical_religious_preference`, `severity` restringido a `mild,moderate,severe_anaphylaxis`, y el controlador (`ClientLimitationController::store`/`update`) devuelve 422 si `type=allergy` sin `severity`. Esta tabla decía que seguía bloqueada pendiente de aplicar en el VPS — no era así, ya estaba aplicada (probablemente junto con el trabajo de integración de onboarding). Queda solo la parte humana (2.3).
- **(2026-09-17) 2.6 — ubicación de la memoria real por cliente, decidida.** El usuario confirmó que los campos de memoria (`observaciones_coach`, `razonamiento`, etc.) deben ser editables a mano, lo que descartó una tabla en Bckbs; entre repo privado de GitHub y Google Sheets, eligió el repo por no requerir integrar la API de Google antes de M1. Creado **`ilzarpeatore/bstronger-memoria-clientes`** (privado), con `clientes/<cliente_id>/` (`perfil-cliente.json`, `perfil-nutricional.json`, `checkpoints-fisicos.json`, `log-registro.json`, `log-nutricion.json`) y una plantilla en `_plantilla/`. Ambos `system-prompt.md` actualizados (entrenamiento v0.11.1, nutrición v0.6.1) con la ruta exacta. Sigue sin datos reales de ningún cliente todavía — la carpeta `clientes/` está vacía a propósito.
- **(2026-09-17) 2.7 — capa de "hábitos prioritarios", construida.** El usuario decidió construirla ya (caso real de Borja + práctica recurrente) y que viviera solo en el agente de nutrición (de los 5 hábitos del caso real, 3 eran nutricionales y 2 de estilo de vida, ninguno de programación de ejercicio). Nuevo módulo `agentes/programacion-nutricion/modulos/habitos-prioritarios.md` (siempre activo, tras fijar el plan) y nuevo campo `habitos_prioritarios` en `esquemas/log-nutricion.schema.json`. `system-prompt.md` de nutrición sube a v0.7.0.
- **(2026-09-19) 2.5 — cerrado del todo.** Decisión de fondo confirmada por el usuario: parchear a mano cliente a cliente (no construir un re-prompt proactivo en la app), verificando primero que las 4 preguntas ya funcionan solas para clientes nuevos. Se confirmó por código (no supuesto) que **no hacía falta ningún cambio de app**: `onboarding_v2_screen.tsx` (registro) y `onboarding_data_screen.tsx` ("Mis respuestas", Perfil) ya recorren `constants/onboardingV2Questions.ts` directamente sin ninguna lista aparte que excluyera las 4 preguntas añadidas el 2026-09-16 — cualquier cliente nuevo desde esa fecha ya las ve, y cualquier cliente antiguo puede editarlas desde "Mis respuestas". El hueco era solo de datos, no de código. Aplicado ahora también a **Hamsa (98)** y **Ayoub (102)**: `parq_eating_disorder_history=false`, `cooking_minutes_per_meal=40`, `cooking_skill_level=intermediate`, `cooks_for_others=false` para ambos — vía SSH real a Bckbs y reflejado en `bstronger-memoria-clientes`. Con Borja/Toni (ya resueltos 2026-09-18), **los 4 clientes que tenían fila real de `par_q_answers`/`nutrition_questionnaire_answers** quedan completos.
- **(2026-09-19) Osas (103) completó el onboarding real (PAR-Q y nutrición) el mismo día — verificado por SSH, filas creadas hoy.** Antes no existía ninguna fila; ahora sí, con un hallazgo de seguridad real: `parq_bone_joint_problem=true`, con texto libre "Operación de tendón rotuliano de la rodilla derecha, operación de labrum de cadera izquierda" (sin fecha ni estado de recuperación). Según `contraindicaciones-medicas.md` (sección 3), esto es una **contraindicación relativa** que bloquea cualquier programación hasta autorización profesional explícita — añadida a `restricciones_salud` (`autorizacion_profesional: false`) y `cribado_medico.resultado` pasa de `"requiere_revision_coach"` (valor que no existe en el enum del esquema, usado antes por falta total de datos) a `"contraindicacion_relativa"` (valor real). Contradice al guideline de coach ("Sin lesiones previas reportadas") — se prioriza el PAR-Q real, más reciente y estructurado. **Bloqueante: falta que el coach confirme fecha/recuperación de ambas cirugías y dé autorización antes de generar cualquier borrador de entrenamiento para Osas.** Alberto Martín (104) sigue siendo el único de los 6 sin ninguna fila de PAR-Q/nutrición.
- **(2026-09-19) 2.4 — regla de derivación de `nivel_fuerza` contrastada y sustituida por confirmación directa del coach.** El coach dio el nivel real de 6 clientes: Hamsa=avanzado, Toni=intermedio, Borja=avanzado (los 3 coinciden con la regla propuesta 2026-09-16), Ayoub=avanzado, Osas=intermedio, Alberto=principiante (los 3 NO coinciden con lo que derivaba la regla: Ayoub tenía la técnica autoevaluada más baja de los 6 —1/10— y aun así es el nivel real más alto de los tres discordantes; Osas y Alberto quedaban sobrestimados). Conclusión: `experiencia_meses`/`tecnica_autoevaluada` por sí solos no predicen el nivel real de forma fiable — 3 de 6 mal. `nivel_fuerza` deja de derivarse siempre en `system-prompt.md` (v0.12.0) y pasa a leerse directamente cuando el coach ya lo confirmó (los 6 clientes reales), derivándose solo como estimación de baja confianza para clientes nuevos sin confirmar. `esquemas/perfil-cliente.schema.json` actualizado.
- **(2026-09-19) `docs/USERS/NUTRICIÓN` y `docs/USERS/RECETARIO` (repo `bsa`) aplicados a los 5 clientes con perfil real.** Mismo tratamiento que ya se dio a los guidelines de entrenamiento: fuente cruzada, nunca autoridad primaria, discrepancias señaladas en `nutricion._pendiente_revision_coach`, nunca resueltas en silencio. Antes de aplicar nada se identificó cuáles de los ~22 archivos de `RECETARIO` son reales (18-28 recetas con macros en `0` = plantilla vacía sin rellenar, detectable por conteo, no solo por el nombre del archivo) y cuáles son el duplicado más completo por cliente. Hallazgos de calidad de datos relevantes, ninguno bloqueante pero sí dignos de que el usuario los revise en origen: **(1)** `nutricion-toni.html` se contradice a sí mismo — marca alergia crítica a fruta/fructosa pero sus propios ejemplos de comida incluyen plátano y miel; se usó `recetario-toni-personalizado.html` (limpio) en su lugar. **(2)** `nutricion-borja.html` es prácticamente idéntico palabra por palabra a `nutricion-toni.html` (mismo TDEE/macros/ejemplos de comida) — plantilla genérica reutilizada, no personalizada. **(3)** `recetario-betanzos-6.html` (el archivo más grande de toda la carpeta, 120KB) sí es de Borja — **corrección (2026-09-19):** se descartó inicialmente por error, asumiendo que describía a otro cliente, hasta que el usuario confirmó que es real. Su contenido añade contexto real no visto en ningún otro documento: trabaja de noche en un pub/bar (recetas etiquetadas "Cena · Pub / sin cocinar"), y declara "Alérgico a frutas". **Ítem 2.9, resuelto el mismo día:** esto YA estaba parcialmente registrado desde el 2026-09-17 como `client_limitations.id=1` ("Intolerancia a pieles de fruta", `type: intolerance`, sin severidad) — esta misma sesión lo había confundido erróneamente con una intolerancia a LACTOSA que Borja nunca tuvo (no existe ninguna otra fila en `client_limitations` para él). El usuario confirmó: es alergia (síndrome de alergia oral — reacciona a la piel cruda, tolera fruta sin piel), severidad leve. Corregido en Bckbs (`id=1`: `type=allergy`, `severity=mild`) y en `bstronger-memoria-clientes`. **(4)** `nutricion-osas.html` (único dato de nutrición real de ese cliente) tiene un error aritmético: los macros declarados no suman ni el total de calorías ni el 100% de los porcentajes. **(5)** El recetario de Alberto declara objetivo "pérdida de grasa" que no coincide con su objetivo real de recomposición corporal ya registrado. El único caso limpio sin reservas fue Hamsa (`nutricion-hamza-dris-v2.html`/`recetario-hamza-dris-v2.html`, ~30 recetas, 100% Halal confirmado por texto). Ver el ítem 2.2 (actualizado) para el trabajo real de recetario que queda pendiente (matching contra el catálogo de Bckbs).
- **(2026-09-19) Fusión de `perfil-cliente.json` + `perfil-nutricional.json` en un solo archivo por cliente.** Decisión del usuario: en vez de dos archivos separados en `bstronger-memoria-clientes`, lo específico de nutrición (gustos, disponibilidad de cocina, nº de comidas) queda anidado bajo la clave `"nutricion"` dentro de `perfil-cliente.json` — nada se duplica, `restricciones_dieteticas`/`objetivos`/`disponibilidad` siguen al nivel raíz, leídos por ambos agentes igual que antes. `esquemas/perfil-nutricional.schema.json` (agente de nutrición) ya no requiere `cliente_id` propio; `esquemas/perfil-cliente.schema.json` (agente de entrenamiento) gana la propiedad opcional `nutricion`. Ambos `system-prompt.md` actualizados (entrenamiento v0.11.2, nutrición v0.8.0) y ambos `CHANGELOG.md`. Migrados los 5 clientes que ya tenían perfil nutricional real (Hamsa, Toni, Hamza Elouafa, Borja, Ayoub) y la plantilla en `_plantilla/` de `bstronger-memoria-clientes`.
- **(2026-09-18/19) Perfiles de Osas (103) y Alberto Martín (104) creados en `bstronger-memoria-clientes`, ambos sin PAR-Q real.** Los dos solo tienen `training_questionnaire_answers` en Bckbs (sin `par_q_answers` ni `nutrition_questionnaire_answers`) — a diferencia del hueco de 2.5 (campos nuevos a `null`), aquí el cribado médico completo está ausente, no solo incompleto. `cribado_medico` queda con todos los campos a `null` y `resultado: "requiere_revision_coach"`, marcado explícitamente como "no debe generarse ningún borrador de entrenamiento hasta completar el PAR-Q real". Ampliados con sus guidelines de coach reales (`docs/USERS/GUIDELINE/guideline-osas.html`, `guideline-alberto.html` + `guideline-alberto-marin.html`, repo `bsa`) siguiendo el mismo criterio ya usado con Hamsa/Toni/Borja: lo que el guideline aporta (contexto, lesiones, condiciones) se documenta en `_pendiente_revision_coach` en texto libre, nunca se estructura en `restricciones_salud`/`client_limitations` sin confirmación humana. Para Alberto esto es más delicado que en los casos anteriores: el guideline señala pubalgia y un trastorno de ansiedad generalizada (TAG), ninguno confirmado todavía por un PAR-Q real ni por el coach — el esquema actual de `perfil-cliente` tampoco tiene un campo pensado para condiciones de salud mental, queda solo como nota. También se detectó que los dos guidelines de Alberto escriben su apellido "Marín"/"Marín Girón" mientras que el registro real en Bckbs (user_id 104) dice "Martín" — asumido como errata de los guidelines, no confirmado. Intentando resolver también el perfil de Josu Mauricio se descubrió que **no tiene ninguna cuenta en Bckbs** (ver ítem 2.8 nuevo) — se quedó sin crear por eso, no por descuido.

---

## Nota (2026-09-16): entorno de pruebas local real

Esta sesión instaló MariaDB local (no Docker, no disponible en el sandbox) y corrió la suite completa de migraciones de Bckbs contra un esquema MySQL real — a diferencia de SQLite, que falla en una migración con sintaxis `ALTER TABLE ... MODIFY` específica de MySQL. Esto permite construir Y probar con tests de verdad cualquier cambio de código (migraciones, modelos, controladores) sin acceso al VPS, dejando solo "aplicarlo en producción" como el paso realmente bloqueado.

1.2, 4.1 y 4.2 se construyeron, probaron (19 tests nuevos en total, MySQL real local) y fusionaron a `main` de Bckbs en dos ramas (`feature/client-limitation-severity`, `feature/import-confidence-gate-check-integrity`), sin conflicto con trabajo de otra sesión que había avanzado `main` mientras tanto. Lo único que sigue bloqueado de verdad es la confirmación contra el VPS real (1.1, y para 1.2 además la migración de producción).

## Mantenimiento de este documento

- Cuando una tarea se resuelva, muévela a "Ya resuelto" con la fecha, no la borres — mismo criterio que los changelogs de cada agente.
- Si aparece una tarea nueva, añádela aquí primero — no crear un nuevo documento de pendientes suelto en otro sitio del repo.
- `docs/roadmap.md` y los `CHANGELOG.md` de cada agente siguen siendo la fuente de verdad de arquitectura e historial de cambios respectivamente; este documento es solo el estado actual de qué queda por hacer.
